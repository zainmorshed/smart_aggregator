# ===================================================================
# NOTES & REFERENCE (not actively used)
# ===================================================================
# spring.application.name=aggregator            # Just metadata, not required
# spring.jpa.hibernate.ddl-auto=update          # Now set via env var SPRING_JPA_HIBERNATE_DDL_AUTO
# spring.security.user.name=zain                # Not used - we use JWT authentication instead
# spring.security.user.password=supersecret     # Not used - we use JWT authentiion instead


# ===================================================================
# ACTIVE CONFIGURATION
# ===================================================================
#db configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/smartaggregator}
#^read from environemnt vairable 'SPRING_DATASOURCE_URL'
#^if it doesn't exist, use the default - local - 5432 = postgresql default port: `jdbc:postgresql://localhost:5432/...`" 
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:yourpassword}


# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}

# Server Configuration
server.port=${PORT:8080}

#local:
# spring.datasource.url=jdbc:postgresql://localhost:5432/smartaggregator
# spring.datasource.username=postgres
# spring.datasource.password=Jakarta39069


# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false 
#^when set to tre it logs all the sql queries to the console (helpful for debugging)
#^so jusdt set to false in production to reduce cluttter, save storage space on logs, avoid loggign sensitive data

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret=${JWT_SECRET:mysecretkey}
#^. openssl rand -base64 32 ---> run in command lin to generate based64 encoded string
#^ JwtUtil uses SECRET_KEY to read from environment 
#token still expires after 1 hour - redployments wont invalidate unexpired tokens now

#API Keys
twelvedata.api.key=${TWELVEDATA_API_KEY}
gnews.api.key=${GNEWS_API_KEY}
#calling CoinGecko - we don't need an API Key for the free tier


